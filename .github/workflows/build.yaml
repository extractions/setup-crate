name: build

on:
  push:
  schedule:
    - cron: 0 12 1 * *

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-node@v6
      with:
        node-version: '24'
    - run: npm ci
    - run: npm run lint
    - run: npm run build
    - run: git diff --exit-code dist/index.js

  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        repo:
          - casey/just
          - catppuccin/whiskers
          - Michael-F-Bryan/mdbook-linkcheck
          - rossmacarthur/sheldon
          - rust-embedded/cross
          - rust-lang/mdBook
          - rustwasm/wasm-bindgen
          - sharkdp/hyperfine
        include:
          # rossmacarthur/powerpack only ships a macos binary
          - os: macos-latest
            repo: rossmacarthur/powerpack
        exclude:
          # rossmacarthur/sheldon does not ship a windows binary
          - os: windows-latest
            repo: rossmacarthur/sheldon
          # macOS aarch64 binaries are not available for mdBook
          - os: macos-latest
            repo: Michael-F-Bryan/mdbook-linkcheck
          # macOS aarch64 binaries are not available for cross
          - os: macos-latest
            repo: rust-embedded/cross

    runs-on: ${{ matrix.os }}
    name: test (${{matrix.os}}, ${{ matrix.repo }})
    steps:
    - uses: actions/checkout@v6
    - name: Setup ${{ matrix.repo }}
      uses: ./
      with:
        repo: ${{ matrix.repo }}
      env:
        ACTIONS_STEP_DEBUG: true
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Test
      run: which $(printf ${{ matrix.repo }} | awk -F '/' '{print $2}' | tr '[:upper:]' '[:lower:]')
      shell: bash
